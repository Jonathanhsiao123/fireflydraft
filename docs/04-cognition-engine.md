# 04 · 認知與推理中樞 / Fable 5 (Overview + TODO)

## Overview
非同步批次引擎。輸入為階層式摘要 (session → week → month) + 檢索式原始片段，非整段歷史灌入 (見 08-A2)。

四大分析任務：
1. 學習曲線與提示依賴分析 → 褪除速度建議
2. 偏好矩陣建模 (MSWO 資料 + 飽足偵測) → 增強物輪換建議
3. 挑戰性行為 ABC 型態挖掘 → 功能假設 (供 FBA 參考)
4. IEP 目標建議草案 (正名：非 IEP 本身，見 08-C5)

幻覺防禦三道閘：
- 自主驗證：模型須以懷疑者角色攻擊自身建議並附上數據引用 (trial_id 級)
- 硬編碼邊界：任何參數變更不得超出 BCBA 預設的安全範圍 (如褪除速度每週最多降一級)
- Human-in-the-loop：StrategyProposal → BCBA 審核佇列 → 電子簽核 → 生效

## TODO
- [ ] 階層摘要器實作 (滾動摘要 + 引用保留)
- [ ] StrategyProposal 結構化輸出 schema + 驗證器 (拒絕缺少證據引用的建議)
- [ ] 自主驗證提示詞範本與紅隊測試 (刻意餵入矛盾數據，驗證是否被識破)
- [ ] BCBA 審核後台 (diff 檢視：目前參數 vs 建議參數 vs 證據)
- [ ] 批次排程器 (每週末離峰執行) + token 用量預估器
